/*
#include "ENCODER.h"

  if(cmd=="ENCODER") {
      ENCODER_A = PARG0(s,1); pinMode(ENCODER_A, INPUT_PULLUP);
      ENCODER_B = PARG0(s,2); pinMode(ENCODER_B, INPUT_PULLUP);
      attachInterrupt(digitalPinToInterrupt(ENCODER_A),ENCODER_INT,CHANGE);
      attachInterrupt(digitalPinToInterrupt(ENCODER_B),ENCODER_INT,CHANGE);
  }

*/