set soft = default5 # имя софта при апгрейде с сервера-матки, не менять!
set password = gluki # пароль админа, если он установлен - только админ сможет редактировать этот файл
set server_update = http://lleo.me/ESP8266/index.php?ip={ip}&chip={chip}&soft={soft} # сервер апгрейда софта

# loop 1 10 upgrade # проверка на апгрейд софта каждые 10 секунд

# НАША РАСПАЙКИ ПИНОВ

# GPIO 0 - FLASH, не должен быть подтянут к земле, заодно CS для FC522 SDA(SS) Chip Select для NFC-читалки
# GPIO 1 - TXD, ничего не подключить сюда
# GPIO 2 - LED --- а пин свободен пока ---
#    blinkpin = 2 # пин мигающего светодиода
#    pinmode {blinkpin} OUTPUT
#    pin {blinkpin} 1
# GPIO 3 - ЗВУК, пин RX, к которому припаяна пищалка
    sound.pin = 5
#    sound.pin.0 = 0 # всегда 0 пока молчит, иначе схема будет греться!
    pinmode {sound.pin} OUTPUT
    pin {sound.pin} 0
# GPIO 4 --- свободен пока ---
#    pinknop = 4
#    pinmode {pinknop} INPUT_PULLUP
# GPIO 5 - RST для NFC-читалки
# GPIO 12 - MISO для NFC-читалки
# GPIO 13 - MOSI для NFC-читалки + МОТОР ВЛЕВО
# GPIO 14 - SCK для сканера + МОТОР ВПРАВО
# GPIO 16 - cs мотора

loopfile {
    /GAVKA
}

# знак системе, что вайфай будем поднимать свой, своими скриптами, а не средставами системы
set WIFI.callback = /WIFI.callback
set WIFI_MY = 1
set AP_name = 5G-Tower-{chip}
set AP_password =
set mdns = DEFAULT3

loop 2 60 /WIFI
/WIFI
